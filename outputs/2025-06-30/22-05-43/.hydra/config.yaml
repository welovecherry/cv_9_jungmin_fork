wandb:
  project: cv-team9
  entity: 12hongcoffee
  name: ${model.name}-${now:%Y-%m-%d-%H-%M-%S}
train:
  seed: 42
  learning_rate: 0.0001
  num_epochs: 10
data:
  path: data/raw
  batch_size: 32
  num_workers: 4
model:
  name: efficientnet_b0
  pretrained: true
